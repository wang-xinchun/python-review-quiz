# Agent 协作记录

## 记录说明

本文件用于记录项目开发过程中与大模型 Agent 的关键协作过程。记录重点包括：用户提示词、开发阶段、Agent 输出摘要、采纳内容、人工判断和验证结果。

## AI-001 选题规划

记录编号：AI-001

使用时间：2026年6月25日

开发阶段：选题规划

使用目标：确定 Python 课程期末大作业项目主题

用户提示词：

```text
我需要你为我完成我的python课程的期末大作业。案例(勿抄).doc 是一个完成后的报告案例，我们最后也要落实成这个形式。同时，这门课有一项很独特的考察，就是对大模型agent使用也会有考察。现在，你不需要写代码，而是和我商论：
1.我们应该做一个什么项目，来展示我对大模型规划的能力以及你的编程能力。
2.我们应该如何规划？如何体现我对大模型应用的水平？
请你认真规划
```

Agent 输出摘要：

建议项目主题为“Python课程智能复习与自测系统”，并提出资料导入、知识点管理、题库管理、自测练习、错题记录、学习统计和报告导出等模块。

采纳内容：

1. 采纳项目主题。
2. 采纳以课程 PPT 为知识来源的方向。
3. 采纳将大模型协作过程写入报告的方案。

人工判断：

用户确认该提案方向合适，要求进一步落实成文档。

验证结果：

已创建项目规划、报告结构、开发任务清单等文档。

## AI-002 项目范围审查

记录编号：AI-002

使用时间：2026年6月25日

开发阶段：范围审查

使用目标：控制项目复杂度，确保尽可能覆盖课程 PPT 基础知识

用户提示词：

```text
请你再针对我们要做的目标内容进行审查。不要过于复杂，但是也不要太简单。尽可能用上PPT中学到的基础知识
```

Agent 输出摘要：

将系统功能划分为必做、应做、可选增强和暂不做四类，并把变量、流程控制、列表、元组、字典、集合、字符串、函数、模块、类、文件操作等课程知识点映射到具体功能中。

采纳内容：

1. 第一版聚焦“题库 - 自测 - 判分 - 错题 - 统计 - 报告导出”闭环。
2. 暂不做登录、在线部署和实时大模型 API。
3. 将 PDF 导入作为增强功能。

人工判断：

该范围既能形成完整项目，又不会偏离 Python 基础课程目标。

验证结果：

已创建 `docs/04_项目范围审查与知识点覆盖.md`，并更新开发任务清单。

## AI-003 GitHub 与工具链规划

记录编号：AI-003

使用时间：2026年6月25日

开发阶段：工程化准备

使用目标：检查 GitHub 插件、创建公共仓库、规划 Codex App skills 和插件使用方式

用户提示词：

```text
我已经把GitHub链接到了我的GitHub，这样我们就能把我们的作业及时git到GitHub。你先检查一下是否可用
```

```text
建一个公共仓库，然后本地进行git，方便我们后面编写代码
```

```text
请你参考一下codex app的官方，看看包括社区提供的skill以及官方提供待下载的插件，有哪些可用于这个项目？
```

Agent 输出摘要：

确认 GitHub 插件可用，创建公共仓库 `python-review-quiz`，初始化本地 git，并整理 Codex skills 和插件使用规划。

采纳内容：

1. 项目仓库命名为 `python-review-quiz`。
2. 使用 GitHub 插件进行版本管理。
3. 使用 PDF、Playwright、文档/PDF/表格等工具辅助后续开发和报告。

人工判断：

课程 PPT 和示例报告不提交到公共仓库，仅提交自有代码和文档。

验证结果：

公共仓库已创建并同步项目文档：`https://github.com/wang-xinchun/python-review-quiz`

## AI-004 最小可运行版本开发启动

记录编号：AI-004

使用时间：2026年6月25日

开发阶段：核心功能开发

使用目标：按照前期文档开始实现系统，并持续记录提示词

用户提示词：

```text
接下来，请你开始按照文档，按照计划，开始落实。同时有意识地记录我的提示词，方便后期整理一下，也是一项加分项
```

Agent 输出摘要：

计划搭建 Streamlit 应用和模块化 Python 代码结构，优先完成题库管理、自测练习、自动判分、错题记录、学习统计和报告导出。

采纳内容：

1. 使用 Streamlit 作为界面展示框架。
2. 使用 SQLite 保存题库、练习记录和答题记录。
3. 使用 JSON 保存初始题库。
4. 使用 Markdown 文档持续记录 Agent 协作过程。

人工判断：

第一版不接入在线大模型 API，不做复杂登录，确保核心功能可运行。

验证结果：

待开发完成后补充运行和测试结果。

## AI-005 测试与问题修复

记录编号：AI-005

使用时间：2026年6月25日

开发阶段：测试与修复

使用目标：验证第一版核心功能，发现并修复运行问题

用户提示词：

```text
接下来，请你开始按照文档，按照计划，开始落实。同时有意识地记录我的提示词，方便后期整理一下，也是一项加分项
```

Agent 输出摘要：

完成 Streamlit 应用、SQLite 数据库模块、初始题库、测试用例和报告导出功能，并使用单元测试、语法检查和 Playwright CLI 进行验证。

采纳内容：

1. 采用模块化代码结构。
2. 使用 42 道初始题目覆盖 7 个章节。
3. 使用 SQLite 保存题库和练习记录。
4. 使用 Playwright CLI 验证页面加载和自测交互。

问题与修复：

测试时发现 SQLite 连接未关闭导致 Windows 临时数据库文件被占用。已将 `get_connection()` 改为上下文管理器，确保提交后关闭连接。

验证结果：

1. 单元测试 5 项通过。
2. `compileall` 语法检查通过。
3. Streamlit 首页正常显示题目数 42、章节数 7。
4. 自测练习页面可以生成题目。
5. 学习报告可以导出。

## AI-006 细节打磨

记录编号：AI-006

使用时间：2026年6月25日

开发阶段：功能完善与细节打磨

使用目标：在第一版可运行基础上继续完善交互和测试覆盖

用户提示词：

```text
很好。请你继续完善、打磨细节
```

Agent 输出摘要：

针对自测练习和报告导出进行细节改进：取消选择题默认选中状态，增加未答题提交校验，让题目内容真正显示在带边框卡片中，并将报告导出模块改为支持传入测试数据库和导出目录。

采纳内容：

1. 自测题目默认不选中答案，避免用户误提交。
2. 提交前检查未作答题目，未完成时提示用户继续作答。
3. 新增未答题检测测试和报告导出测试。
4. 更新 README、测试记录和协作记录。

人工判断：

这些改动不增加项目复杂度，但明显提升演示体验和可靠性。

验证结果：

1. 自动化测试增加到 7 项并全部通过。
2. `compileall` 语法检查通过。
3. 使用 Playwright CLI 验证自测页面交互：题目默认不选中答案，未作答提交会出现提示。
4. 浏览器验证过程中发现一次 Streamlit 热重载导入旧模块问题，重启服务后修复并记录到问题修复文档。

## AI-007 前端界面优化

记录编号：AI-007

使用时间：2026年6月26日

开发阶段：界面优化与视觉验收

使用目标：在不增加系统复杂度的前提下优化 Streamlit 前端界面，使项目展示效果更适合作为课程期末大作业

用户提示词：

```text
请你优化前端界面
```

后续提示词：

```text
请你继续
```

Agent 输出摘要：

围绕现有 Streamlit 应用做轻量界面改造：统一页面头部、侧边栏品牌区、指标卡、筛选区、题目卡、题型标签、统计报告页 Tab，并保持原有题库、自测、判分、错题和报告导出逻辑不变。

采纳内容：

1. 新增全局 CSS 设计风格，统一背景、卡片、按钮、边框和文字层级。
2. 首页改为更清晰的学习仪表盘，使用自定义指标卡展示题目数、章节数、练习次数、正确率和错题数。
3. 自测练习页加入题型、难度和题号标签，提升答题可读性。
4. 学习统计与报告页加入“统计图表 / 报告预览”标签页，报告内容更便于查看。
5. 使用 Playwright CLI 验证桌面首页、自测页、报告页和移动端首页。

人工判断：

本轮优化不改变核心业务逻辑，只改善可读性和展示效果；适合作为课程演示与最终报告截图素材。

验证结果：

1. `compileall` 语法检查通过。
2. 自动化测试 7 项通过。
3. Playwright CLI 验证首页首屏、移动端首页、自测未作答提示和报告预览 Tab。
4. 控制台无 error；存在少量 Vega 图表 warning，不影响核心功能和页面渲染。

## AI-008 使用 playwright-interactive 交互式优化

记录编号：AI-008

使用时间：2026年6月26日

开发阶段：前端细节复查与交互式视觉调优

使用目标：尝试使用 `playwright-interactive` 控制本地浏览器，直接观察页面并继续优化前端展示效果

用户提示词：

```text
[$playwright-interactive](C:\Users\11983\.codex\skills\playwright-interactive\SKILL.md) 你可以尝试控制我的电脑，去优化前端效果
```

Agent 输出摘要：

使用 `node_repl` 启动 Playwright 交互浏览器。由于 Playwright 自带 Chromium 尚未安装，改用本机 Microsoft Edge 通道打开本地 Streamlit 应用。通过桌面和移动端视口截图继续观察界面，发现默认 Streamlit 工具栏和红色控件主色影响展示一致性，并发现报告预览用纯文本框呈现不够像成品。

采纳内容：

1. 新增 `.streamlit/config.toml`，统一 Streamlit 主色为蓝色，并设置背景、侧栏和文字色。
2. 隐藏 Streamlit 默认 `Deploy`、菜单和页脚等展示干扰项。
3. 将报告预览由纯文本框改为 Markdown 渲染预览，并保留“查看 Markdown 原文”入口。
4. 使用交互式 Playwright 检查桌面首页、报告页、自测页和移动端首页。

人工判断：

这轮优化让应用更像独立完成的课程作品，减少了框架默认痕迹，同时保留了报告文本的可复制和可导出能力。

验证结果：

1. `compileall` 语法检查通过。
2. 自动化测试 7 项通过。
3. 交互式 Playwright 检查显示：顶部 `Deploy` 文本已不可见，页面无横向滚动。
4. 报告预览 Tab 能显示渲染后的 Markdown 报告，下载和导出按钮仍存在。
5. 移动端 390px 宽度下首页标题、指标卡和图表入口正常显示。

## AI-009 前端细节复查与警告清理

记录编号：AI-009

使用时间：2026年6月26日

开发阶段：前端细节复查

使用目标：继续优化界面细节，减少不必要依赖和控制台警告，使前端验收结果更干净

用户提示词：

```text
请你继续优化，检查细节
```

Agent 输出摘要：

使用前端 QA 和 `playwright-interactive` 继续检查页面。发现首页章节图表由 Altair/Vega 渲染时控制台存在 warning，虽然不影响功能，但不利于最终验收记录。将图表替换为纯 HTML/CSS 条形列表，保留原有数据表格，并移除项目中不再直接使用的 Altair 依赖。

采纳内容：

1. 使用 CSS 条形列表替换 Altair 条形图。
2. 章节题量、章节正确率和错题分布继续保留可视化展示。
3. 移除 `requirements.txt` 中不再直接使用的 `altair` 依赖。
4. 使用交互式 Playwright 验证首页、统计页、自测页和移动端首页。

人工判断：

该改动降低了前端依赖复杂度，也消除了控制台 warning，让项目演示和测试记录更干净。

验证结果：

1. `compileall` 语法检查通过。
2. 自动化测试 7 项通过。
3. 首页、统计页和移动端检查均无 console error/warning。
4. 自测页题目默认不选中，未作答提交提示仍正常。
